version: "3.8"
services:
  api:
    build:
      context: .
    volumes:
      - .:/app
      - /app/node_modules  # preserva node_modules do build (evita sobrescrever com o mount)
    ports:
      - "3333:3333"

